# Default values for helm-charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

fullnameOverride : ""
image:
  repository: "102355283528.dkr.ecr.eu-west-1.amazonaws.com/next-payments-dev-ecr-o7hpzt982ux7-repository-6fn20nl9eito"
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "e4095b62"

container:
  name: "next-payment"
  services: []

hasendpoint: true

service:
  type: LoadBalancer
  port: 80
  name: api
  targetport: 80
  namespace: default

ingress:
  enabled: false
  annotations: 
    kubernetes.io/ingress.global-static-ip-name: ""
    networking.gke.io/managed-certificates: app-cert-managment
    networking.gke.io/v1beta1.FrontendConfig: frontendconfig
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: ""
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: api
            port:
              number: 3000

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

environment:
  env:
    PORT: 80
    NODE_ENV: development
    NAMESPACE: mp
    MC_TRANSPORTER_TYPE: "NATS"
    MC_TRANSPORTER: "{\"servers\":\"nats://nats-1658401573:4222\"}"
    PAYTR_MERCHANT_ID: 114147
    PAYTR_MERCHANT_KEY: B7hAE774pz3U4rgY
    PAYTR_MERCHANT_SALT: 2DA22p9kqs6LypaT
    PAYTR_PAYMENT_ACCOUNT: 2323832000011272031
    PAYTR_MODE: Auto CreditCard
    PAYTR_DEV_URL: "https://webhook.site/342838b9-de0e-4adc-87bd-da6d6db6ff61"
    CHARGE_RESPONSE_TIMEOUT: 30
    MOLECULER_APM_ENABLE: 1
    TEST_PAYMENT_ENABLE: 1
    PAYTR_BANK_CHARGES_RATE: "0.0314"
    FAWATERAK_API_KEY: 8abf1e152baf4a424f835284bbc90916f1a9a957d75fe4efc8
    FAWATERAK_BASE_URL: "http://fawaterkstage.com/api/v2"
  secret:
    MONGO_URI: "mongodb+srv://next-payment-db-usr-dev:wBSwfYxbGqMIPsPP@dev.tehlk.mongodb.net/test?retryWrites=true&w=majority"

laboratory:
  enabled: false
  name: lab
  type: LoadBalancer
  port: 3210
  targetport: 3210

cron:
  enabled: false
  list:
    - name: products-cron
      schedule: "*/5 * * * *"
      command: products.updateAll
    - name: orders-cron
      schedule: "*/30 * * * *"
      command: orders.updateAll

graphql:
  enabled: false
  name: graphql
  type: NodePort
  port: 8000
  targetport: 8000